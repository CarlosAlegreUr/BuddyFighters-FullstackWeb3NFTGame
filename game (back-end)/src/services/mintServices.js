async function requestRandomStats(playerAddress, nftId) {
    // Give permission to call VRF to client.
}

async function requestMint(playerAddress, nftId) {
    // Check nums generated, generate URI.
    // Give permission to call Mint with correct URI.
}

module.exports = {
    requestRandomStats,
    requestMint,
};
